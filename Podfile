# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SSScanDoc' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SSScanDoc
  pod 'WeScan'
  pod 'PDFGenerator'

  target 'SSScanDocTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SSScanDocUITests' do
    # Pods for testing
  end

end
